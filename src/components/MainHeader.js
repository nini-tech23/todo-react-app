import classes from "../index.module.css";
const MainHeader = () => {
    return (
        <header className={classes.mainheadercontainer}>
            <h1 className={classes.mainheader}>Task Management Board</h1>
        </header>
    )
};
export default MainHeader;